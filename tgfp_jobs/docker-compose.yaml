services:
  tgfp-run-jobs:
    image: johnofcamas/tgfp-job-runner
    container_name: tgfp-run-jobs
    env_file:
      - stack.env
    restart: unless-stopped
    entrypoint: prefect worker start --pool 'tgfp-jobs'
    volumes:
      - ./scripts:/app/scripts
      - /tmp:/backup
